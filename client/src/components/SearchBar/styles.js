import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    searchbar: {
        margin: '30px 0',
        display: 'flex',
        maxWidth: 1000,
        justifyContent: 'center',
        alignItems: 'center',
    },
}));